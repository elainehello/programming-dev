<persistence xmlns="https://jakarta.ee/xml/ns/persistence"
             version="3.0">
    <persistence-unit name="accountPU">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Entities will go here -->
        <!-- <class>com.acme.platform.account.domain.Account</class> -->

        <class>org.acme.platform.account.domain.Account</class>
        <class>org.acme.platform.account.domain.PurchaseOrder</class>

        <properties>
            <property name="jakarta.persistence.jdbc.driver" value="org.h2.Driver"/>

            <!-- updated h2 database -->
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:h2:file:./data/accountdb;DB_CLOSE_DELAY=-1"/>

            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>

            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>
